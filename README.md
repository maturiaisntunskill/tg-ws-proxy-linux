# Telegram Desktop WebSocket Bridge Proxy (Rust)
```
Usage: tg-ws-proxy [OPTIONS]

Options:
      --port <PORT>    [default: 1080]
      --host <HOST>    [default: 127.0.0.1]
      --dc-ip <DC:IP>  
  -v, --verbose        
  -h, --help           Print help
```
